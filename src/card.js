import React from "react";

const Card = () => {
  return (
    <article className="card">
      <header className="card__header">
        <h2 className="card__heading">Foo</h2>
      </header>
    </article>
  );
};

export default Card;
